# Check whether MazamaSpatialUtils has been initialized

Convenience function that checks whether
\`initializeMazamaSpatialUtils()\` has been run in the current R
session.

## Usage

``` r
spatialIsInitialized()
```

## Value

Logical \`TRUE\` if initialization has been performed in the current
session, \`FALSE\` otherwise.
